import 'package:flutter/material.dart';

import 'AddAndRemove.dart';
import 'ShowBooking.dart';

/*class CPanelRoutingPage extends StatelessWidget {
  int selectedOrder;
  CPanelRoutingPage(this.selectedOrder);
  @override
  Widget build(BuildContext context) {
    switch (selectedOrder) {
      case 0:
        {
          return AddAndRemove(this.selectedOrder);
        }
        break;
      case 1:
        {
          return AddAndRemove(this.selectedOrder);
        }
        break;
      case 2:
        {
          return ShowBooking();
        }
    }
  }
}
*/
